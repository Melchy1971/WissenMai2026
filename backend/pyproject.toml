[project]
name = "wissensbasis-backend"
version = "0.1.0"
description = "FastAPI backend fuer Wissensbasis V1"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "psycopg[binary,pool]",
    "sqlalchemy",
    "alembic",
    "python-multipart",
    "python-docx",
    "pypdf",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = ["--strict-markers"]
markers = [
    "postgres: requires a real PostgreSQL database - set TEST_DATABASE_URL or use docker-compose.test.yml",
    "postgres_truth: hard PostgreSQL truth tests; only skip when TEST_DATABASE_URL is missing",
    "m4a_gate: critical M4a gate evidence; must run as postgres_truth",
    "m4b_gate: critical M4b gate evidence; must run as postgres_truth",
    "m4c_gate: critical M4c gate evidence; must run as postgres_truth",
]
